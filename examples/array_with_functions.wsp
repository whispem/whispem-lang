fn sum_array(arr) {
    return reduce(arr, fn(acc, n) { return acc + n }, 0)
}

fn find_max(numbers) {
    return reduce(numbers, fn(acc, n) { if n > acc { return n } return acc }, numbers[0])
}

let nums = [5, 2, 9, 1, 7, 3]

print "Sum:"
print sum_array(nums)

print "Max:"
print find_max(nums)
