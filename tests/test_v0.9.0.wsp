# Whispem v0.9.0 - Comprehensive Test

print "=== Array Basics ==="
let numbers = [1, 2, 3, 4, 5]
print numbers

let first = numbers[0]
print first

numbers[2] = 10
print numbers

print "=== Array Functions ==="
let len = length(numbers)
print len

let new_arr = push(numbers, 6)
print new_arr

print "=== Array Iteration ==="
let items = [10, 20, 30]
let i = 0
while i < length(items) {
    print items[i]
    let i = i + 1
}

print "=== Arrays with Functions ==="
fn sum(arr) {
    let total = 0
    let i = 0
    while i < length(arr) {
        let total = total + arr[i]
        let i = i + 1
    }
    return total
}

let result = sum([1, 2, 3, 4, 5])
print result

print "=== Mixed Types ==="
let mixed = [1, "hello", true]
print mixed

print "=== Nested Arrays ==="
let nested = [[1, 2], [3, 4]]
print nested
print nested[0]
print nested[1][0]

print "=== Dynamic Building ==="
fn make_array(n) {
    let arr = []
    let i = 0
    while i < n {
        let arr = push(arr, i * 2)
        let i = i + 1
    }
    return arr
}

let dynamic = make_array(5)
print dynamic

print "=== String Length ==="
let text = "Whispem"
let text_len = length(text)
print text_len

print "All tests passed!"
